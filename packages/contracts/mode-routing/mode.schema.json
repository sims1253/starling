{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:starling:contracts:mode-routing:mode:v1",
  "$comment": [
    "Starling mode-routing contract: one typed, versioned mode entry (E18).",
    "A mode is DATA. It names intent, behavior, the leading-phrase routing",
    "flags, scoped vocabulary/snippets, the selection role, delivery policy",
    "and processing-route descriptors. It cannot encode permission elevation:",
    "privacy/transmission grants belong to runtime and user policy and are",
    "frozen at activation (MODES_AND_CONTEXT.md); no mode field adds",
    "providers, grants or network endpoints.",
    "Alias gating (E26): aliases are leading spoken phrases matched only at",
    "the start of a take after permitted whitespace, on whole-token",
    "boundaries, with a finite separator set (space or colon). They fire only",
    "when the session allows aliases, the take is not a locked manual choice,",
    "and the resolved profile sets allow_spoken_overrides true. Verbatim",
    "modes set allow_spoken_overrides false by default, so dictated words",
    "stay literal in them. Longest unambiguous alias wins (token count, then",
    "character length); an alias shared across modes at equal best rank is a",
    "conflict needing resolution, never a guess. A leading 'literal' escape",
    "selects verbatim payload handling ahead of any alias.",
    "This file validates a single mode entry. profiles.schema.json embeds the",
    "identical entry schema as its profiles items and adds the surrounding",
    "resolution document (default profile plus scoped rules);",
    "tests/test_mode_routing.py asserts the two cannot drift.",
    "Keyword subset: only keywords the repo's minischema.py fallback",
    "understands are used, so jsonschema and minischema agree. Cross-field",
    "rules a JSON Schema cannot express (unique ids, valid default, verbatim",
    "profile present, local-only vs remote route, replace_selection requires",
    "edit-target authority, alias uniqueness) are enforced by the executable",
    "oracle tests/mode_routing.py validate_config.",
    "Processing (#293): transform_kinds, language, style, spoken_commands,",
    "context_fields and processing_delivery describe WHAT to do with the",
    "text, never WHO does it: the authoring_route names a route, and a",
    "provider declaration (provider.schema.json) says which route it",
    "serves. tests/mode_routing.py processing_route picks at most one",
    "provider and never falls back to another one; local_only never",
    "reaches a remote provider.",
    "Delivery: save_note is history only (nothing is inserted anywhere);",
    "copy puts the text on the clipboard; insert_enter inserts and then",
    "presses Enter, for chat/agent targets. insert_enter is explicit",
    "opt-in: validate_config refuses it on the default profile and with",
    "edit-target authority, and the runtime delivery machine still has",
    "no command that presses anything."
  ],
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "version",
    "name",
    "description",
    "intent",
    "behavior",
    "prompt_file",
    "aliases",
    "allow_spoken_overrides",
    "vocabulary",
    "snippets",
    "asr_route",
    "authoring_route",
    "local_only",
    "selected_text",
    "selection_required",
    "clipboard",
    "max_context_characters",
    "delivery",
    "automatic_delivery",
    "transform_kinds",
    "language",
    "style",
    "spoken_commands",
    "context_fields",
    "processing_delivery"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.-]+$",
      "minLength": 1
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Version of this mode definition; decisions record it as mode_version."
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string"
    },
    "intent": {
      "enum": [
        "dictate",
        "edit",
        "ask",
        "capture"
      ]
    },
    "behavior": {
      "enum": [
        "faithful",
        "verbatim",
        "code_guidance",
        "edit_selection",
        "ask_selection",
        "capture"
      ]
    },
    "prompt_file": {
      "type": [
        "string",
        "null"
      ]
    },
    "aliases": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Enabled leading spoken phrases (code this, edit this). Leading-position, whole-token matches only."
    },
    "allow_spoken_overrides": {
      "type": "boolean",
      "description": "Verbatim flag: false disables this mode's aliases and keeps leading phrases literal. Verbatim modes ship false by default."
    },
    "vocabulary": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Scoped recognition-bias terms for this mode; never a provider or permission."
    },
    "snippets": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "spoken",
          "expansion"
        ],
        "properties": {
          "spoken": {
            "type": "string",
            "minLength": 1
          },
          "expansion": {
            "type": "string",
            "minLength": 1
          }
        }
      },
      "description": "Scoped spoken-to-text expansions applied in this mode only."
    },
    "asr_route": {
      "type": "string"
    },
    "authoring_route": {
      "type": [
        "string",
        "null"
      ]
    },
    "local_only": {
      "type": "boolean"
    },
    "selected_text": {
      "enum": [
        "off",
        "reference",
        "edit_target"
      ]
    },
    "selection_required": {
      "type": "boolean"
    },
    "clipboard": {
      "type": "boolean"
    },
    "max_context_characters": {
      "type": "integer",
      "minimum": 0
    },
    "delivery": {
      "enum": [
        "insert",
        "replace_selection",
        "preview",
        "save_note",
        "copy",
        "insert_enter"
      ]
    },
    "automatic_delivery": {
      "type": "boolean"
    },
    "transform_kinds": {
      "type": "array",
      "items": {
        "enum": [
          "clean",
          "format",
          "rewrite",
          "translate"
        ]
      },
      "description": "Processing kinds this mode asks for, in one model step. Empty means transcribe only: the raw recognition is the output. Providers declare which kinds they support (provider.schema.json); a mode never names a provider."
    },
    "language": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]{2,8})*$",
      "description": "Declared dictation language (BCP 47). null means undeclared: only providers that accept any language may process it."
    },
    "style": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "formality",
            "structure",
            "context"
          ],
          "properties": {
            "formality": {
              "enum": [
                "casual",
                "semi-casual",
                "semi-formal",
                "formal"
              ]
            },
            "structure": {
              "enum": [
                "prose",
                "lists"
              ]
            },
            "context": {
              "enum": [
                "general",
                "email"
              ]
            }
          }
        }
      ],
      "description": "Closed style axes for clean/format. They map one to one onto the S1-mini control line ([Styling] [Structure] [Context]); instruction providers render them into their prompt."
    },
    "spoken_commands": {
      "type": "boolean",
      "description": "Deterministic spoken punctuation/layout commands (new line, comma, bullet) run before any model step. A leading 'literal' keeps the next command word as text."
    },
    "context_fields": {
      "type": "array",
      "items": {
        "enum": [
          "personal_context",
          "vocabulary"
        ]
      },
      "description": "Which scoped context fields a transform request may carry to the provider. The request records exactly the fields it sent."
    },
    "processing_delivery": {
      "enum": [
        "direct",
        "staged"
      ],
      "description": "direct: raw text is delivered immediately and a processed revision may replace it only while the target is unchanged. staged: output waits in the draft until the user accepts a revision."
    }
  }
}
