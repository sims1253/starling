# Python / venv
.venv/
.venv*/
__pycache__/
*.pyc
*.egg-info/

# uv cache / state
.uv/

# runtime artifacts
models/
outputs/
traces/
golden/
*.trace.json
*.pt
*.safetensors
*.gguf

# Audio samples (never check these in)
*.wav
*.flac
*.mp3
# ...except the one public fixture source (LibriSpeech 2086-149220-0033, CC BY 4.0):
# tests/fixtures/make_fixtures.py builds short/medium/long.wav from it.
!tests/fixtures/2086-149220-0033.wav
# ...and its copy bundled into the Android e2e test APK.
!apps/mobile/app/src/androidTest/assets/librispeech-2086-149220-0033.wav

# regenerable benchmark corpora (downloaded + cached on first run)
tests/fixtures/leaderboard_corpus/

# internal coordination / runtime locks (not for publication)
comms.md
review.md
.gpu.lock

# internal dev notes (kept locally, untracked — see docs/native-serving.md,
# docs/ggml-engine.md, docs/hf-gguf-readme.md for the published docs)
docs/competitor-lessons.md
docs/ggml-roadmap.md
docs/ggml-fused-ops-spec.md
docs/ggml-optimization-ideas.md
docs/ggml-moss-spec.md
docs/ggml-ark-perf.md
docs/ggml-parakeet-perf-analysis.md
docs/ggml-inrepo-baseline.md
docs/ggml-moss-goldens.md
docs/gpu-isolation-and-parakeet-parity.md

# agent-tool working state (accidentally swept in by a git add . in 918da17)
.claude/
.zcode/
plans/

# test/lint caches
.pytest_cache/
.ruff_cache/

# notebooks / editor
.ipynb_checkpoints/

# downloaded model checkpoints (never check these in)
.hf-cache/

# ggml patch-apply flock sentinel (scripts/apply_ggml_patches.sh)
third_party/.ggml-patch.lock

# CMake build outputs and directories
build*/
cmake-build-*/
*.so
*.a
*.dylib
*.dll
*.exe

# Distribution / release binaries and archives (packaged at repo root)
/starling-serve*
artifacts/
*.tensors.json

dbg/
.hf-cache

# Application workspaces
node_modules/
dist/
target/
.gradle/
local.properties
playwright-report/
test-results/
*.tsbuildinfo

# Apple build products and user state
.build/
DerivedData/
*.xcuserstate
xcuserdata/
apps/ios/*.xcodeproj/

apps/mobile/app/.cxx/
benchmarks/fast_engine/.layout_eval_cache/
# autoresearch loop scratch state (local only)
.auto/
