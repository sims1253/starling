# Python / venv
.venv/
__pycache__/
*.pyc
*.egg-info/

# uv cache / state
.uv/

# runtime artifacts
models/
outputs/
traces/
golden/
*.trace.json

# Audio samples (never check these in)
*.wav
*.flac
*.mp3

# regenerable benchmark corpora (downloaded + cached on first run)
tests/fixtures/leaderboard_corpus/

# internal coordination / runtime locks (not for publication)
comms.md
.gpu.lock

# test/lint caches
.pytest_cache/
.ruff_cache/

# notebooks / editor
.ipynb_checkpoints/
